{
 "name": "bogs-e2e",
 "private": true,
 "scripts": {
  "test": "playwright test",
  "test:ui": "playwright test --headed",
  "test:rules": "firebase emulators:exec --only firestore --project bogs-pedidos \"node rules.test.js\""
 },
 "devDependencies": {
  "@playwright/test": "^1.50.0",
  "@firebase/rules-unit-testing": "^4.0.1",
  "firebase": "^11.0.0"
 }
}